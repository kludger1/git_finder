In this project I used async, await, and fetch api to
get the users and their repos from githubs api.

The way this app work is the client types in a github
user in the input and fetches and pulls that users 
info from githubs api.